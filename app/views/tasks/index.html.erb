<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
      <li><%= link_to task.title, task %>
      <%= link_to "Delete", task,
            method: :delete,
            data: { confirm: "Are you sure?" } %>
      </li>
    <% end %>
</ul>
<br>
<br>

<%= link_to "Add new task", tasks_new_path %>
